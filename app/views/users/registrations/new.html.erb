<h2>Sign up</h2>

<%= form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f| %>
  <%= devise_error_messages! %>

<div class="field">
<%= f.label :ユーザー名 %><br>
<%= f.text_field :name, autofocus: true, autocomplete: "name", placeholder: "ユーザー名を入力して下さい" %>
</div>
  <div class="field">
    <%= f.label :メールアドレス %><br />
    <%= f.email_field :email, autofocus: true, autocomplete: "email", placeholder: "メールアドレスを入力して下さい" %>
  </div>

  <div class="field">
    <%= f.label :パスワード（半角英数字6桁以上） %><br />
    <%= f.password_field :password, autocomplete: "new-password", placeholder: "パスワードを入力して下さい" %>
  </div>

  <div class="field">
    <%= f.label :パスワード確認 %><br />
    <%= f.password_field :password_confirmation, autocomplete: "new-password", placeholder: "パスワードをもう一度入力して下さい" %>
  </div>

  <div class="actions">
    <%= f.submit "Sign up" %>
  </div>
<% end %>

<%= render "users/shared/links" %>
